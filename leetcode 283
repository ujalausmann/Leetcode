void moveZeroes(int* nums, int numsSize) {
    int last = 0;  // index for placing next non-zero

    for (int i = 0; i < numsSize; i++) {
        if (nums[i] != 0) {
            int temp = nums[i];
            nums[i] = nums[last];
            nums[last] = temp;
            last++;
        }
    }
}
