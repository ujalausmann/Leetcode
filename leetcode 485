int findMaxConsecutiveOnes(int* nums, int numsSize){
    int maxCount = 0, curr = 0;

    for (int i = 0; i < numsSize; i++) {
        if (nums[i] == 1)
            curr++;
        else
            curr = 0;

        if (curr > maxCount)
            maxCount = curr;
    }

    return maxCount;
}
